Guide one climate-monitor acquisition run before report preparation.

Load the immutable run binding and follow its source scope, date policy, and budgets. Reference the configured `search_guidance`, `relevance`, `article_summary`, and `executive_summary` components by their bound IDs and hashes; do not copy their text into this task.

Use the installed native Hermes search capability directly when observed coverage gaps justify it. Use the existing web-listening acquisition interface for controlled fetches and climate_registry.acquisition for history, idempotent batch storage, readback, and frozen report input. Record actual tool/engine attempts, queries, result references, concise decision/retry reasons, budget use, missing coverage, publication-date evidence, and next steps.

Adapt to results: inspect history and scope, fetch candidates, assess body and coverage, search or retry only when justified and within budget, store the exact batch, read it back, then freeze compatible report input. A failed search is not zero results. A defensible no-search decision needs a concrete coverage reason. Unknown publication dates remain pending review under an enabled window. Never use event, discovery, issue, or fetch dates as publication dates.

Completion is established only by validated tool results, stored content versions, batch/coverage evidence, and a frozen report input accepted by the existing prepare path. Do not claim completion from prose. Do not publish, deliver, bypass the report gates, create a queue, or introduce another search/fetch implementation.
